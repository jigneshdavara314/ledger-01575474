# Secrets — NEVER commit these
.env
.venv/

# Python caches
__pycache__/
*.pyc

# Regenerable backfill cache (rebuilt on demand from the API)
polybot/backfill_bets_cache.json

# NOTE: trades.db is intentionally NOT ignored — GitHub Actions commits it
# back each run so paper-trade history persists in the cloud.
.claude/
