requests>=2.31.0
python-dotenv>=1.0.0
# AI fair-value estimation (optional but recommended)
openai>=1.40.0
# Real (LIVE) trading on Polymarket — official client
py-clob-client>=0.19.0
# AI estimator (Claude) — declared so it doesn't silently downgrade to heuristic
anthropic>=0.40.0
# Testing (CI gates the bot cycle on a green test run)
pytest>=8.0.0
# Big-data edge validation (query the 1.1B-trade archive without full download)
duckdb>=1.0.0
pandas>=2.0.0
